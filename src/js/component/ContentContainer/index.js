import styled from 'react-emotion'
import {
  maxGridWidth,
} from 'js/styles'

const ContentContainer = styled('DIV')`
max-width: ${maxGridWidth}px;
width: 100%;
margin: 0 auto;
display: block;
`

export {
  ContentContainer,
}
